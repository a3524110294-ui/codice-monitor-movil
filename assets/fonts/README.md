# Fonts directory

This folder is reserved for the custom app fonts used by the UI.

To complete the font setup, add the following files here:
- Geist-Regular.ttf
- Geist-Bold.ttf
- GeistMono-Regular.ttf

Then run:

    flutter pub get

The `pubspec.yaml` file is already configured to register these font families.
